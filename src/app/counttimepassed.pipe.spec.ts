import { CountimePassed } from './counttimepassed.pipe';

describe('RemoveHyphensPipe', () => {
  it('create an instance', () => {
    const pipe = new CountimePassed();
    expect(pipe).toBeTruthy();
  });
});
